#
# This file is required.
#
# Give your command-handler an action name,
# specify which domain it's for,
# and set which version of the event to create.
#

name: some-action
domain: some-domain
service: core

devDependencies:
  "mocha": "^7.1.0"
  "chai": "^4.2.0"
  "sinon": "^7.3.2"
  "sinon-chai": "^3.3.0"
  "chai-datetime": "^1.5.0"
  "@blossm/request": "*"

#don't change
procedure: post-job
