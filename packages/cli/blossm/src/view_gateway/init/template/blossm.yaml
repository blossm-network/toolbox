#
# This file is required.
# It's URL will be auth.[network]
#

domain: some-domain
service: core

stores:
  - name: some-name
    priviledges:
      - some-priviledge
  - name: some-other-name
    protected: false

whitelist:
  - "127.0.0.1"

devDependencies:
  "mocha": "^6.1.4"
  "chai": "^4.2.0"

#don't change
procedure: view-gateway
