// require("localenv");
const { expect } = require("chai");

// const request = require("@blossm/request");
// const uuid = require("@blossm/uuid");

// const url = `http://${process.env.MAIN_CONTAINER_NAME}`;

describe("View store integration tests", () => {
  it("Nothing to test", async () => {
    expect(1).to.equal(1);
  });
  // const id = uuid();
  // it("should return successfully", async () => {
  //   const response0 = await request.put(`${url}/${id}`, {
  //     body: {}
  //   });
  //   expect(response0.statusCode).to.equal(204);
  //   const response1 = await request.get(`${url}/${id}`);
  //   const parsedBody1 = JSON.parse(response1.body);
  //   expect(response1.statusCode).to.equal(200);
  //   expect(parsedBody1[property].length).to.equal(1);
  //   expect(parsedBody1[property].permissions[0]).to.equal(value);
  // });
});
