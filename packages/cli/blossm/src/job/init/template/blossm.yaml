#
# Give your job a name,
# and optionally specify which domain and service it's for.
#
name: some-action
domain: some-optional-domain
service: some-optional-service

#
# Add dependencies and devDependencies, just like a package.json file.
#
devDependencies:
  "chai-datetime": "^1.5.0"
  "@blossm/datetime": "*"

#don't change
procedure: job
