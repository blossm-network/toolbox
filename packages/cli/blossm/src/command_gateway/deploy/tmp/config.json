{
  "domain": "session",
  "service": "core",
  "commands": [{ "action": "start", "protected": false }],
  "whitelist": ["127.0.0.1", "https://${process.env.NETWORK}"],
  "context": "command-gateway",
  "testing": {
    "procedures": [],
    "topics": [
      "did-start.session.core.local",
      "did-issue.challenge.core.local",
      "did-answer.challenge.core.local"
    ]
  }
}
