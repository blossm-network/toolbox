{
  "name": "@blossm/authentication-middleware",
  "version": "0.0.114",
  "description": "Authentication middleware for the network.",
  "main": "index.js",
  "author": "blossm.network",
  "engines": {
    "node": ">= 10.16.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "mocha --recursive"
  },
  "dependencies": {
    "@blossm/authenticate": "^0.0.79",
    "@blossm/gcp-kms": "^0.0.35",
    "@blossm/tokens-from-req": "^0.0.18",
    "express-async-handler": "^1.1.4"
  },
  "devDependencies": {
    "mocha": "^7.1.1"
  }
}
