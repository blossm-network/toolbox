{
  "name": "@blossm/authentication-middleware",
  "version": "0.0.65",
  "description": "Authentication middleware for the network.",
  "main": "index.js",
  "author": "blossm.network",
  "engines": {
    "node": ">= 10.16.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "mocha --recursive"
  },
  "dependencies": {
    "@blossm/authenticate": "^0.0.43",
    "@blossm/gcp-kms": "^0.0.26",
    "express-async-handler": "^1.1.4"
  },
  "devDependencies": {
    "mocha": "^7.1.0"
  }
}
