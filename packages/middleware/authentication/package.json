{
  "name": "@blossm/authentication-middleware",
  "version": "0.0.19",
  "description": "Authentication middleware for the network.",
  "main": "index.js",
  "author": "blossm.network",
  "engines": {
    "node": ">= 10.16.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "mocha --recursive"
  },
  "dependencies": {
    "@blossm/authenticate": "^0.0.7",
    "@blossm/gcp-kms": "^0.0.13",
    "express-async-handler": "^1.1.4"
  },
  "devDependencies": {
    "mocha": "^6.1.4"
  }
}
