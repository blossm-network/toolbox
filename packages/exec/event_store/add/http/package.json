{
  "name": "@sustainer-network/event-store-add-service",
  "description": "Add to an event store.",
  "version": "0.0.1",
  "scripts": {
    "test": "mocha --recursive",
    "check-packages": "yarn clean && yarn compile && yarn test"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@sustainer-network/errors": "*",
    "@sustainer-network/event-bus": "*",
    "@sustainer-network/event-store": "^0.0.23",
    "@sustainer-network/validator": "^0.0.3"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "sinon": "^7.3.2",
    "sinon-chai": "^3.3.0"
  }
}
