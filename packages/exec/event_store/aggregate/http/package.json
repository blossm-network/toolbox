{
  "name": "@sustainers/event-store-aggregate-service",
  "description": "Hydrate an event store aggregate root.",
  "version": "0.0.31",
  "scripts": {
    "test": "mocha --recursive",
    "check-packages": "yarn clean && yarn compile && yarn test"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@sustainers/errors": "^0.0.6",
    "@sustainers/event-store": "^0.0.28",
    "@sustainers/validator": "^0.0.4"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "sinon": "^7.3.2",
    "sinon-chai": "^3.3.0"
  }
}
