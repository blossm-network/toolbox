{
  "name": "@sustainers/event-store-aggregate-service",
  "description": "Hydrate an event store aggregate root.",
  "version": "0.0.5",
  "scripts": {
    "test": "mocha --recursive",
    "check-packages": "yarn clean && yarn compile && yarn test"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@sustainers/errors": "^0.0.5",
    "@sustainers/event-store": "^0.0.3",
    "@sustainers/validator": "^0.0.3"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "sinon": "^7.3.2",
    "sinon-chai": "^3.3.0"
  }
}
