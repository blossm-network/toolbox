{
  "name": "@sustainer-network/event-store-aggregate-service",
  "description": "Hydrate an event store aggregate root.",
  "version": "0.0.1",
  "scripts": {
    "test": "mocha --recursive",
    "check-packages": "yarn clean && yarn compile && yarn test"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@sustainer-network/errors": "*",
    "@sustainer-network/event-store": "^0.0.24",
    "@sustainer-network/validator": "^0.0.4"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "sinon": "^7.3.2",
    "sinon-chai": "^3.3.0"
  }
}
