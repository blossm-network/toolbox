action: add-identities
domain: group
service: core

payload:
  identities:
    type: stringArray

devDependencies:
  "mocha": "^6.1.4"
  "chai": "^4.2.0"
  "sinon": "^7.3.2"
  "sinon-chai": "^3.3.0"
  "chai-datetime": "^1.5.0"

testing:
  examples:
    ok:
      - payload:
          identities: [some-identity-root]
        normalized:
          name: [some-identity-root]

  invalid:
    - identities: 3

  pre:

  procedures:

  response:

#don't change
context: command-handler
