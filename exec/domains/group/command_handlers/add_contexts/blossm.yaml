action: add-contexts
domain: group
service: core

payload:
  contexts:
    type: stringArray

devDependencies:
  "mocha": "^6.1.4"
  "chai": "^4.2.0"
  "sinon": "^7.3.2"
  "sinon-chai": "^3.3.0"
  "chai-datetime": "^1.5.0"

testing:
  examples:
    ok:
      - payload:
          contexts: [some-context-root]
        normalized:
          contexts: [some-context-root]

  invalid:
    - contexts: 3

  pre:

  procedures:

  response:

#don't change
context: command-handler
