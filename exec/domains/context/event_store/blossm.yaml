domain: context
service: core

schema:
  group: String
  root: String
  domain: String
  service: String
  network: String

testing:
  examples:
    - action: register
      payload:
        group: some-group-root
        root: some-context-root
        domain: some-domain
        service: some-service
        network: some-network
    - action: register
      payload:
        group: some-other-group-root
        root: some-other-context-root
        domain: some-other-domain
        service: some-other-service
        network: some-other-network

  handlers:
    - action: register
      examples:
        - payload:
            group: some-group-root
            root: some-context-root
            domain: some-domain
            service: some-service
            network: some-network

#don't change
context: event-store
