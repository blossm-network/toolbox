name: overview
domain: context
service: core
memory: 256Mi

schema:
  name: String
  identity: String

indexes: [[{ identity: 1 }]]

devDependencies:
  "localenv": "^0.2.2"
  "mocha": "^6.1.4"
  "chai": "^4.2.0"
  "sinon-chai": "^3.3.0"
  "@blossm/request": "*"
  "@blossm/uuid": "*"

testing:
  examples:
    id:
      first:
        - put:
            identity: some-identity
            name: some-name
          get:
            identity: some-identity
            name: some-name
      second:
        - put:
            identity: some-other-identity
            name: some-other-name
          get:
            identity: some-other-identity
            name: some-other-name
    index:
      put:
        name: some-name
        identity: some-identity
      context:
        identity: some-identity
      get: 
        name: some-name
        identity: some-identity
    stream:
      first:
        put:
          name: some-name
          identity: some-identity
        get: 
          name: some-name
          identity: some-identity
      second:
        put:
          name: some-other-name
          identity: some-other-identity
        get: 
          name: some-other-name
          identity: some-other-identity
      context:
        identity: some-identity

#don't change
context: view-store
