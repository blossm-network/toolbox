name: overview
domain: context
service: core
memory: 256Mi

schema:
  name: String
  identity: String

indexes: [[{ identity: 1 }]]

devDependencies:
  "localenv": "^0.2.2"
  "mocha": "^6.1.4"
  "chai": "^4.2.0"
  "sinon-chai": "^3.3.0"
  "@blossm/request": "*"
  "@blossm/uuid": "*"

testing:
  examples:
    - name: some-name
      identity: some-identity
    - name: some-other-name
      identity: some-other-identity

#don't change
context: view-store
