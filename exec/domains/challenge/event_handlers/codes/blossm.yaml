#
# This file is required.
#
# Give your event-handler the action that it's subscribed to, with it's domain,
# and a unique name that describes it.
#

name: codes
action: issue
domain: challenge
service: core
version: 0

dependencies:
  "@blossm/view-store-rpc": "*"
  "@blossm/gcp-token": "*"
  "@blossm/datetime": "*"

devDependencies:
  "localenv": "^0.2.2"
  "mocha": "^6.1.4"
  "chai": "^4.2.0"
  "sinon": "^7.3.2"
  "sinon-chai": "^3.3.0"
  "chai-datetime": "^1.5.0"

testing:
  procedures:
    - name: codes
      domain: challenge
      context: view-store
  
  examples:
    - root: some-root

      payload:
        code: some-code
        expires: 3600

      results:
        - store:
            name: codes
            domain: challenge
          root: some-root 
          value: 
            code: some-code
            expires:

#don't change
context: event-handler
