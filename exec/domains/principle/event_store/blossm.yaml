domain: principle
service: core

schema:
  permissions: [String]

testing:
  examples:
    - action: "add"
      payload:
        permissions: [some-permission, some-other-permission]
    - action: "remove"
      payload:
        permissions: [some-permissions]

#don't change
context: event-store
