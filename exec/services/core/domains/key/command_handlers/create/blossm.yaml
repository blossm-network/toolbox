name: create
domain: key
service: core

payload:
  name: string

devDependencies:
  "mocha": "^6.1.4"
  "chai": "^4.2.0"
  "sinon": "^7.3.2"
  "sinon-chai": "^3.3.0"
  "chai-datetime": "^1.5.0"

testing:
  validate:
    ok:
      name: Some-name
    bad:
      name: 3

  normalize:
    - payload:
        name: Some-name
      normalized:
        name: Some-name
    - payload:
        name: some-name
      normalized:
        name: some-name

  dependencies:

  steps:
    pre:
    payload:
    context:
    claims:
    root:
    response:

#don't change
procedure: command-handler
