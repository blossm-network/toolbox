domain: session
service: core

commands:
  - name: start
    protected: false
  - name: save
    priviledges: none
  - name: logout
    priviledges:
      - update
  - name: change-scene
    priviledges:
      - update

whitelist:
  - "127.0.0.1"

devDependencies:
  "mocha": "^6.1.4"
  "chai": "^4.2.0"

#don't change
procedure: command-gateway
